{
  "name": "<%- name %>",
  "version": "<%- version %>",
  "main": "<%- main %>",
  "private": true,
  "scripts": {
<% if (proj_type === 'ts') { %>
    "build": "tsc -p .",
<% } %>
    "start": "tabris serve"
  }
}
